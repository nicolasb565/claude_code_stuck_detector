.DS_Store
node_modules/
logs/

# Python
__pycache__/
*.pyc
.venv/

# Large data files (reproducible from HuggingFace + labeling pipeline)
data/
# Compressed train/test splits are small enough to track
!data/train_windows.jsonl.gz
!data/test_id.jsonl.gz

# PyTorch checkpoints (cnn_weights.json is the portable format)
proxy/cnn_checkpoint.pt
proxy/cnn_trimmed_checkpoint.pt
proxy/cnn_streak_checkpoint.pt

# Derived benchmark output
proxy/benchmark_summary.json
